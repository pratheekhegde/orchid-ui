// initial state
const state = {
  campaign: {
    apiUrl: "campaign"
  },
  content: {
    apiUrl: "content"
  },
  publisher: {
    apiUrl: "publisher"
  }
};

// getters
const getters = {};

// actions
const actions = {};

// mutations
const mutations = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
};
